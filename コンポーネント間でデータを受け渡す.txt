親：LikeNumber.vue
子：App.vue

親コンポーネントから子コンポーネントに変数を受け渡す
１，親コンポーネントでpropsを配列で定義
２，属性として定義
３，propsはthisでアクセスできる
※propsではキャメルケースで定義して、HTML内で使用するときはケバブケースで書くのが一般的
理由：HTMLは大文字と小文字を区別できないため


子コンポーネントから親コンポーネントに変数を受け渡す
$emitでデータを受け渡す
１，
２，
３，